<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.pratclot.oxygentracker">

    <application
        android:name=".OxygenTracker"
        android:allowBackup="true"
        android:icon="@drawable/ic_baseline_mood_48"
        android:label="@string/app_name"
        android:roundIcon="@drawable/ic_baseline_mood_48"
        android:supportsRtl="true"
        android:theme="@style/Theme.OxygenTracker">
        <activity
            android:name="com.pratclot.oxygentracker.activities.OxygenActivity"
            android:label="@string/app_name"
            android:theme="@style/Theme.OxygenTracker.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>